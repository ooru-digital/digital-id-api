apiVersion: v1
kind: Service
metadata:
  name: {{ include "digitalid-backend.fullname" . }}
  labels:
    {{- include "digitalid-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digitalid-backend.selectorLabels" . | nindent 4 }}
